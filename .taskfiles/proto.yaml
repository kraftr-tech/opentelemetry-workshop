version: "3.0"

tasks:
  bank:
    desc: Generate protobuf files for bank-service
    sources:
      - proto/transaction.proto
    generates:
      - src/bank-service/pb/transaction.pb.go
      - src/bank-service/pb/transaction_grpc.pb.go
    cmds:
      - mkdir -p src/bank-service/pb
      - |
        protoc -Iproto \
          --go_out=src/bank-service/pb --go_opt=paths=source_relative \
          --go-grpc_out=src/bank-service/pb --go-grpc_opt=paths=source_relative \
          proto/transaction.proto
